var age = prompt("Please enter your age: ");

if (age < 0)
{
	alert("Please, come back when you are out of the womb!");
}

if (age % Math.sqrt(age) == 0)
	{
		alert("Your age is a perfect square");
	}
